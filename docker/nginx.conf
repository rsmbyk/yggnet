server {
	listen 80;
	server_name _;
	root /usr/share/nginx/html;
	index index.html;

	# Hashed SvelteKit assets
	location /_app/immutable/ {
		try_files $uri =404;
		add_header Cache-Control "public, max-age=31536000, immutable";
	}

	location / {
		try_files $uri $uri/ /index.html;
	}
}
