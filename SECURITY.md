# Security

- No secrets in the repository.
- Client-side import of JSON / attachments must enforce size and type limits (SPECs).
- Sanitize or strictly validate untrusted graph documents before applying to domain state.
- Report vulnerabilities privately to the project maintainers (do not open a public issue with exploit detail until a fix is available).
